[package]
name = "rust_lib"
version = "0.1.0"
edition = "2021"

[lib]
# 動的ライブラリ(dll/so)としてコンパイルする設定
crate-type = ["cdylib"]

[dependencies]
jni = "0.21.1" #
minacalc-rs = { version = "0.4.0", features = ["hashmap", "thread", "rox", "utils"] }